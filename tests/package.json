{
  "name": "bloom-events-tests",
  "private": true,
  "description": "Browser checks for the Bloom Events site. Serve the site locally first (see README).",
  "scripts": {
    "booking": "node booking-flow.js",
    "calendar": "node calendar-flow.js",
    "owner": "node owner-flow.js",
    "site": "node site-check.js",
    "test": "node booking-flow.js && node calendar-flow.js && node owner-flow.js && node site-check.js"
  },
  "devDependencies": {
    "axe-core": "4.10.3",
    "fullcalendar": "6.1.21",
    "playwright": "1.56.1"
  }
}
